# ===========================================
# LARAVEL PRODUCTION ENVIRONMENT CONFIGURATION
# ===========================================
# This file contains production environment variables for your expense tracker application.
# IMPORTANT: Replace all placeholder values with your actual production settings.

# ===========================================
# APPLICATION SETTINGS
# ===========================================
APP_NAME="Expense Tracker"
APP_ENV=production
APP_KEY=base64:wT5B6kwhzOjtyOcb6Ah74Yj53kkyoND1fqsm6Wepo+o=
APP_DEBUG=true
APP_URL=https://et.kmnazmul.com/

# Application Locale Settings
APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

# Application Maintenance Mode
APP_MAINTENANCE_DRIVER=file
APP_MAINTENANCE_STORE=database

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
DB_CONNECTION=mysql
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=odbhutco_accounts
DB_USERNAME=odbhutco_accounts
DB_PASSWORD=&*gbGRsf$f789*(

# Database Charset and Collation
DB_CHARSET=utf8mb4
DB_COLLATION=utf8mb4_unicode_ci

# Database Foreign Key Constraints
DB_FOREIGN_KEYS=true

# MySQL SSL Configuration (if needed)
# MYSQL_ATTR_SSL_CA=/path/to/ca-cert.pem

# ===========================================
# CACHE CONFIGURATION
# ===========================================
# Cache driver: array, database, file, memcached, redis, dynamodb, octane
CACHE_STORE=redis
CACHE_PREFIX=expense-tracker-cache-

# Database Cache (if using database cache)
DB_CACHE_CONNECTION=mysql
DB_CACHE_TABLE=cache
DB_CACHE_LOCK_CONNECTION=mysql
DB_CACHE_LOCK_TABLE=cache_locks

# ===========================================
# SESSION CONFIGURATION
# ===========================================
# Session driver: file, cookie, database, memcached, redis, dynamodb, array
SESSION_DRIVER=redis
SESSION_LIFETIME=120
SESSION_EXPIRE_ON_CLOSE=false
SESSION_ENCRYPT=false
SESSION_CONNECTION=redis
SESSION_TABLE=sessions
SESSION_STORE=redis
SESSION_COOKIE=expense-tracker-session
SESSION_PATH=/
SESSION_DOMAIN=yourdomain.com
SESSION_SECURE_COOKIE=true
SESSION_HTTP_ONLY=true
SESSION_SAME_SITE=lax
SESSION_PARTITIONED_COOKIE=false

# ===========================================
# REDIS CONFIGURATION
# ===========================================
REDIS_CLIENT=phpredis
REDIS_CLUSTER=redis
REDIS_PREFIX=expense-tracker-database-
REDIS_PERSISTENT=false

# Redis Default Connection
REDIS_URL=redis://127.0.0.1:6379
REDIS_HOST=127.0.0.1
REDIS_USERNAME=null
REDIS_PASSWORD=null
REDIS_PORT=6379
REDIS_DB=0
REDIS_MAX_RETRIES=3
REDIS_BACKOFF_ALGORITHM=decorrelated_jitter
REDIS_BACKOFF_BASE=100
REDIS_BACKOFF_CAP=1000

# Redis Cache Connection
REDIS_CACHE_CONNECTION=cache
REDIS_CACHE_DB=1
REDIS_CACHE_LOCK_CONNECTION=default

# ===========================================
# MAIL CONFIGURATION
# ===========================================
MAIL_MAILER=smtp
MAIL_HOST=mail.kmnazmul.com
MAIL_PORT=465
MAIL_ENCRYPTION=ssl
MAIL_USERNAME=system@kmnazmul.com
MAIL_PASSWORD=Kdy5Yf@wwXLZxcB
MAIL_FROM_ADDRESS="system@kmnazmul.com"
MAIL_FROM_NAME="Expense Tracker"

# Mail URL (alternative to individual settings)
# MAIL_URL=smtp://username:password@smtp.your-provider.com:587

# Mail Scheme (http or https)
MAIL_SCHEME=https

# Mail EHLO Domain
MAIL_EHLO_DOMAIN=yourdomain.com

# Mail Sendmail Path (if using sendmail)
# MAIL_SENDMAIL_PATH=/usr/sbin/sendmail -bs -i

# Mail Log Channel (if using log driver)
# MAIL_LOG_CHANNEL=mail

# ===========================================
# QUEUE CONFIGURATION
# ===========================================
# Queue driver: sync, database, beanstalkd, sqs, redis, null
QUEUE_CONNECTION=redis
QUEUE_FAILED_DRIVER=database-uuids

# Database Queue (if using database driver)
DB_QUEUE_CONNECTION=mysql
DB_QUEUE_TABLE=jobs
DB_QUEUE=default
DB_QUEUE_RETRY_AFTER=90

# Redis Queue (if using redis driver)
REDIS_QUEUE_CONNECTION=default
REDIS_QUEUE=default
REDIS_QUEUE_RETRY_AFTER=90

# Beanstalkd Queue (if using beanstalkd driver)
# BEANSTALKD_QUEUE_HOST=localhost
# BEANSTALKD_QUEUE=default
# BEANSTALKD_QUEUE_RETRY_AFTER=90

# ===========================================
# AWS CONFIGURATION (if using AWS services)
# ===========================================
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key
# AWS_DEFAULT_REGION=us-east-1
# AWS_BUCKET=your-s3-bucket
# AWS_USE_PATH_STYLE_ENDPOINT=false

# DynamoDB Cache (if using DynamoDB cache)
# DYNAMODB_CACHE_TABLE=cache
# DYNAMODB_ENDPOINT=https://dynamodb.us-east-1.amazonaws.com

# SQS Queue (if using SQS)
# SQS_PREFIX=https://sqs.us-east-1.amazonaws.com/your-account-id
# SQS_QUEUE=default
# SQS_SUFFIX=

# ===========================================
# MEMCACHED CONFIGURATION (if using Memcached)
# ===========================================
# MEMCACHED_PERSISTENT_ID=your_persistent_id
# MEMCACHED_USERNAME=your_username
# MEMCACHED_PASSWORD=your_password
# MEMCACHED_HOST=127.0.0.1
# MEMCACHED_PORT=11211

# ===========================================
# LOGGING CONFIGURATION
# ===========================================
LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error

# ===========================================
# BROADCASTING CONFIGURATION (if using broadcasting)
# ===========================================
# BROADCAST_DRIVER=log
# PUSHER_APP_ID=your_pusher_app_id
# PUSHER_APP_KEY=your_pusher_app_key
# PUSHER_APP_SECRET=your_pusher_app_secret
# PUSHER_HOST=your_pusher_host
# PUSHER_PORT=443
# PUSHER_SCHEME=https
# PUSHER_APP_CLUSTER=mt1

# ===========================================
# SANCTUM CONFIGURATION (if using API authentication)
# ===========================================
# SANCTUM_STATEFUL_DOMAINS=localhost,127.0.0.1,127.0.0.1:8000,::1
# SESSION_DOMAIN=localhost

# ===========================================
# VITE CONFIGURATION (for asset compilation)
# ===========================================
VITE_APP_NAME="${APP_NAME}"
# VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
# VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

# ===========================================
# SECURITY RECOMMENDATIONS
# ===========================================
# 1. Generate a new APP_KEY: php artisan key:generate
# 2. Use strong, unique passwords for all services
# 3. Enable HTTPS in production (SESSION_SECURE_COOKIE=true)
# 4. Use Redis or database for sessions in production
# 5. Set APP_DEBUG=false in production
# 6. Use environment-specific database credentials
# 7. Regularly rotate your APP_KEY and database passwords
# 8. Monitor your logs for any security issues
